# node_modules is hundreds of MB and is reinstalled inside the image anyway;
# shipping the host's copy also risks platform-specific binaries leaking into a
# Linux image. dist/ is regenerated by the build.
node_modules/
dist/
dist-ssr/
.vite/
coverage/
*.log
